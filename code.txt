SELECT COUNT(*) AS Total_Employees
FROM EMPLOYEES;

SELECT COUNT(*) AS Total_Departments
FROM DEPARTMENTS;

SELECT SUM(SALARY) AS Total_Salary
FROM EMPLOYEES;

SELECT SUM(COMMISSION_PCT) AS Total_Commission
FROM EMPLOYEES;

SELECT AVG(SALARY) AS Avg_Salary
FROM EMPLOYEES;

SELECT AVG(MIN_SALARY) AS Avg_Min_Salary,
       AVG(MAX_SALARY) AS Avg_Max_Salary
FROM JOBS;

SELECT MAX(SALARY) AS Highest_Salary,
       MIN(SALARY) AS Lowest_Salary
FROM EMPLOYEES;

SELECT MAX(MAX_SALARY) AS Highest_Max_Salary,
       MIN(MAX_SALARY) AS Lowest_Max_Salary
FROM JOBS;
SELECT DEPARTMENT_ID,
       COUNT(*) AS Num_Employees
FROM EMPLOYEES
GROUP BY DEPARTMENT_ID
ORDER BY DEPARTMENT_ID;

SELECT JOB_TITLE,
       AVG(MIN_SALARY) AS Avg_Min,
       AVG(MAX_SALARY) AS Avg_Max
FROM JOBS
GROUP BY JOB_TITLE;

SELECT DEPARTMENT_ID,
       COUNT(*) AS Num_Employees
FROM EMPLOYEES
GROUP BY DEPARTMENT_ID
ORDER BY DEPARTMENT_ID;

SELECT JOB_TITLE,
       AVG(MIN_SALARY) AS Avg_Min,
       AVG(MAX_SALARY) AS Avg_Max
FROM JOBS
GROUP BY JOB_TITLE;

SELECT DEPARTMENT_ID,
       COUNT(*) AS Num_Employees
FROM EMPLOYEES
GROUP BY DEPARTMENT_ID
HAVING COUNT(*) > 5
ORDER BY DEPARTMENT_ID;

SELECT JOB_TITLE,
       AVG(MAX_SALARY) AS Avg_Max_Salary
FROM JOBS
GROUP BY JOB_TITLE
HAVING AVG(MAX_SALARY) > 10000
ORDER BY Avg_Max_Salary DESC;

--1:
SELECT E.EMPLOYEE_ID,
       E.FIRST_NAME || ' ' || E.LAST_NAME AS Full_Name,
       D.DEPARTMENT_NAME
FROM EMPLOYEES E
JOIN DEPARTMENTS D ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;

--2:
SELECT E.FIRST_NAME || ' ' || E.LAST_NAME AS Full_Name,
       J.JOB_TITLE,
       E.SALARY
FROM EMPLOYEES E
JOIN JOBS J ON E.JOB_ID = J.JOB_ID;

-- 3:
SELECT E.FIRST_NAME || ' ' || E.LAST_NAME AS Full_Name,
       D.DEPARTMENT_NAME,
       L.CITY
FROM EMPLOYEES E
JOIN DEPARTMENTS D ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
JOIN LOCATIONS L ON D.LOCATION_ID = L.LOCATION_ID;

--4:
SELECT E.FIRST_NAME || ' ' || E.LAST_NAME AS Full_Name,
       D.DEPARTMENT_NAME,
       C.COUNTRY_NAME,
       R.REGION_NAME
FROM EMPLOYEES E
JOIN DEPARTMENTS D ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
JOIN LOCATIONS L ON D.LOCATION_ID = L.LOCATION_ID
JOIN COUNTRIES C ON L.COUNTRY_ID = C.COUNTRY_ID
JOIN REGIONS R ON C.REGION_ID = R.REGION_ID;

--5:
SELECT E.FIRST_NAME || ' ' || E.LAST_NAME AS Full_Name,
       D.DEPARTMENT_NAME
FROM EMPLOYEES E
LEFT JOIN DEPARTMENTS D ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;
--6:
